# Database Schema

This directory contains the core user management schema for the Home Management Portal.

## Files

- `v1_init.sql`: Initializes `profiles` and `user_roles` tables for authentication and authorization.

> **Note**: The schema for rental contracts and properties has been moved to `generator-najemnich-smluv/database` to be co-located with the application code.
